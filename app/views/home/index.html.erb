<div ng-controller="IndexController" class="blog-header col-md-12">
  <h1 class="blog-title text-center">{{ title }}</h1>
  <div class="row col-md-8" ng-repeat="post in data.posts">
    <h2>{{ post.title }}</h2>
    <p>{{ post.contents }}</p>
  </div>
</div>
